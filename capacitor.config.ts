import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shrutiranjandev.tutorialide',
  appName: 'Tutorial IDE',
  webDir: 'dist'
};

export default config;
